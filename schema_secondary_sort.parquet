message titleBasics {
    required binary tconst (STRING);
    required group titles (LIST){
        repeated group title{
            required binary type (STRING);
            required binary primary (STRING);
            required binary original (STRING);
        }
    }
    optional group genres (LIST){
        repeated binary array (STRING);
    }
   required float averageRating;
}